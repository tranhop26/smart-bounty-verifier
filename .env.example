# Leave every value blank until you deploy the reviewed contract source.
# The UI independently compares the deployed source with the bundled source
# before it enables wallet writes.
VITE_STUDIONET_CONTRACT_ADDRESS=
VITE_BRADBURY_CONTRACT_ADDRESS=
VITE_ASIMOV_CONTRACT_ADDRESS=
VITE_LOCALNET_CONTRACT_ADDRESS=
